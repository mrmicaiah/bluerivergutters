---
layout: base.njk
extraCss: city-pages.css
---

{# Page sections live in partials/city-body.njk and are included by each child
   template, so the child's {% set %} content blocks are in scope. See templates/STANDARDS.md. #}
{{ content | safe }}
