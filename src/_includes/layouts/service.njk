---
layout: base.njk
extraCss: service-pages.css
---

{# Page sections live in partials/service-body.njk and are included by each child
   template, so the child's {% set %} content blocks are in scope. See templates/STANDARDS.md. #}
{{ content | safe }}
