# This file is intentionally NOT a data source.
#
# Eleventy reads project data from src/_data/projects.json (the official location).
# This folder is for the project detail TEMPLATE only (project.njk).
#
# An old duplicate JSON used to live here and caused confusion in April 2026 —
# updates were being pushed to the orphan file while the live site read from _data/.
# Don't recreate it.
